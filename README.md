# @hvntapp/prettier-config

This should be used in all HVNT TS/JS projects

## Usage

### Install:
`$ npm i --save-dev @hvntapp/prettier-config`

### Edit `package.json`:
```
{
  // ...
  "prettier": "@hvntapp/prettier-config"
}
```
